<div class="col-md-12">
  <div class="group">
    <% link_to("修改", edit_group_path(@group), class: "btn btn-parimary pull-right")%>
  </div>
  <h2><%= @group.title %></h2>
  <p><%= @group.description %></p>
</div>
